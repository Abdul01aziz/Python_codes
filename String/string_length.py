def str_len(s):
    """
    Returns the length of the given string.

    Args:
        s (str): The input string.

    Returns:
        int: The length of the string.
    """
    count = 0
    for char in s:
        count += 1
    return count
